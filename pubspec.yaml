name: livi_bank_test
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this line
    sdk: flutter         # Add this line
  cupertino_icons: ^1.0.2
  # hive: ^2.0.4
  # Hive DataBase
  intl: ^0.17.0
  hive: ^2.0.4
  hive_flutter: ^1.0.0
  twilio_phone_verify: ^0.0.2
  provider: ^5.0.0
  country_code_picker:
    git:
      url: https://github.com/Klaus-Wong/country_code_picker.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Hive DataBase Generators
  build_runner:
  hive_generator: ^1.1.0

flutter:
  uses-material-design: true
